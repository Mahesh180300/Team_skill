{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "buildCommand": "cd skill-frontend && npm install && chmod +x node_modules/.bin/vite && npm run build",
  "outputDirectory": "skill-frontend/dist",
  "installCommand": "echo 'skip root install'"
}
